# Suites SQL legacy que corren en CI (job db-tests-legacy de ci.yml).
# ⚠️ Directorio en extinción: las suites nuevas van en supabase/tests/ (pgTAP).
# Cada suite migrada a pgTAP se elimina de este manifest y de tests-legacy/.
#
# Requisito para entrar en esta lista: la suite debe ser AUTO-VERIFICANTE —
# cada bloque corre en BEGIN...ROLLBACK y lanza EXCEPTION "Xn FALLÓ: ..." si
# hay regresión. Éxito = el archivo entero corre sin error (psql ON_ERROR_STOP).
#
# Desde 2026-07-14 las 11 suites cumplen el formato y corren igual contra el
# stack local (supabase db reset + seed.sql) que contra el proyecto real.
squad_formats.sql
transfer_history.sql
